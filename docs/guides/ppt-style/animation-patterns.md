# Animation Patterns (Archived Entry)

Canonical source:

- `guidelines/ppt-style/animation-patterns.md`

Key rule:

- Core text must remain visible without external trigger classes.

Use this archived entry for docs navigation; edit the canonical source file for content changes.
